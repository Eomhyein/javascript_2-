// Map
// key / value
// key에 어떤 데이터 타입(유형)도 다 들어올 수 있다.
// map은 키가 정렬된 순서로 저장되기 때문이다.
// 검색, 삭제, 제거, 여부 확인

const myMap = new Map();
myMap.set('key', 'value');

myMap.get('key')
// 반복 method : keys(), values(), entries()
// for ... of

